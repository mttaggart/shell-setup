theme = "dracula"

[editor]

[editor.soft-wrap]
#enable = true
max-wrap = 25 # increase value to reduce forced mid-word wrapping
max-indent-retain = 0
wrap-indicator = ""  # set wrap-indicator to "" to hide it

[keys.insert]
capslock = "normal_mode"

[keys.normal]
capslock = "insert_mode"
